apiVersion: 1

providers:
  - name: "CarinClaim Dashboards"
    folder: "CarinClaim"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
